/*
 * HM Pro Blocks – editor bootstrap
 *
 * Keep this file lightweight. Block-specific editor scripts live inside each block.
 */

( function ( wp ) {
	if ( ! wp || ! wp.domReady ) {
		return;
	}

	wp.domReady( function () {
		// Reserved for future editor bootstrapping.
	} );
} )( window.wp );
