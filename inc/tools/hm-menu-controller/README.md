# HM Menu Controller

Admin sidebar menu visibility controller (UI-only).  
This plugin intentionally does not restrict access/capabilities; it only manages what appears in the admin sidebar and plugin list UI.
